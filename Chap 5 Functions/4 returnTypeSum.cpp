#include <iostream>
using namespace std;

int sum (int a, int b) { // int sum is a return type 
    return a+b;          // it means that at the end return is mandatory
}

int main () {
    cout << sum(45,64); //  sum(45,64)-- it is an integer which have a specific value
}