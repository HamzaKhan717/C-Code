#include <iostream>
using namespace std;
int main () {
    int x , y;    // we cannot use float here
    x = 4;       // we use modulus operator for checking no. that one number is divisible by other no.
    y = 2;
    cout <<x%y;  // modulus (%) actually gives us reminder of a number if x divide by y (x/y)

}