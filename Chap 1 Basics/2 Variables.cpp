#include <iostream>
using namespace std;
int main (){
    // float x;   //this is called declaration
    // x = 4;     //this is called Initialization
    // float y;
    // y = 8;
    // cout<< x/y; //instead of division (/)I can also use(+,-,*,/)
    

    // //we can also write it like this
    // float w,z;
    // w = 8;
    // z = 6;
    // cout <<w-z;

    //another way to write
    int a=5, b=3;  //indentation and initioalization both are in the same line
    cout <<a*b;
}