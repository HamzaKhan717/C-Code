#include <iostream>
using namespace std;
int main (){
    int x=6;
    cout<<x<<endl;

    // x = 8;
    x += 8;              //this is the alternative of the above one
    cout << x <<endl;

    // x = x + 5;
    x -= 5;             //this is the alternative of the above one
    cout<<x<<endl;

    // x = x + 5;
    x /= 5;            //this is the alternative of the above one
    cout<<x<<endl;

    // x = x * 7;
    x *= 7;           //this is the alternative of the above one



}