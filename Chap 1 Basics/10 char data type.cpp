#include <iostream>
using namespace std;
int main () {
    // char s = 'w';    it is the character data type
    // cout <<s;

    // char ch;           // we use it for finding ASCII values
    // cin>>ch;           //it is the code for finding  ASCII values
    // cout<<(int)ch;    // we made character to integer  
    //                  // We input: (a----z, A---Z , !,@,#,$,%,^,* etc) and output ((40,50,55,100,83)  
    //  OR      
                
      char ch;
      cin>>ch;
      int x=(int)ch;
      cout<<x;


    // int q;          //we just put number (40,50,55,100,83 etc) and that give us
    // cin >>q;         //the characters on that number
    // char ch = (char) q;  
    // cout <<ch;           //Here we made integer to character
    //   // We input(40,50,55,100,83 etc) and get output  (a----z, A---Z , !,@,#,$,%,^,* etc
      



    

}