#include <iostream>
using namespace std;
int main () {
    //               ALLOWED

    //  1   Variable can start from an alphabet and numbers after alphabet
    //      All from      A------ Z  and  1,2,3,4,5,6,7,8,9..... we can use no.  after alphabets
    //      e.g  a, b, c, x, y, z,   or   a1, a4, c7, h3 .....                                                     
    //  2   we can use    underscore ( _ ) and $ 
    //  3   we can use keyword that start from capital letters

    //               NOT ALLOWED

    //  Space   and   comma(,) are  are not allowed
    //  we can not use data type names as a variable
    //  e.g int , float, auto, double, break, extern, unsigned, while, case, enum, sizeof, for, const, 
    //   static, long, continue, else, signed, do, short, switch, char, volatile, default, goto, struct,
    //   if, union, return, void, 
}